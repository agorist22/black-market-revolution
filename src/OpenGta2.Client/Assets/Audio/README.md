# Pulse audio cues (Week 1)

Drop CC0 / original one-shots here using cue-sheet ids:

| Path | Cue ID |
|------|--------|
| `ui/ui_wanted_tick.wav` | `ui_wanted_tick` |
| `stinger/stinger_wanted_alert.wav` | `stinger_wanted_alert` |
| `ui/ui_nap_break.wav` | `ui_nap_break` |

Until files exist, `AudioCueService.Play` stubs with console + `DiagnosticValues` (`audio.cue`, `audio.<id>=stub`).

See `docs/audio/CUE-SHEET-V0.md`.
